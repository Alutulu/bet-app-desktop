import React from "react";
import Navigation from "../components/Navigation";
import SoccerButton from "../components/SoccerButton";

const Home = () => {
    return (
        <div>
            <Navigation />
            <h1>ACCUEIL</h1>
            <SoccerButton />
        </div>
    );
};

export default Home;
